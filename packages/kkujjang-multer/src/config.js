export const config = {
  // 최대 파일명(확장자 포함) 길이
  fileNameLength: 200,
  // 필드 이름의 최대 Byte
  fieldNameSize: 100,
  // text value의 총합 최대 Byte
  fieldSize: 1024 * 1024,
  // text filed의 총합 개수
  fields: 50,
}
